import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  CategoryCard: {
    backgroundColor: "#851a77",
    height: "8rem",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    textTransform: "capitalize",
    borderRadius: "0.62rem",
  },
});
